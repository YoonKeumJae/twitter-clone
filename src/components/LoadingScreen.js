import { Wrapper, Text } from "../styles/StyledLoadingScreen";

const LoadingScreen = () => {
  return (
    <Wrapper>
      <Text>Loading...</Text>
    </Wrapper>
  );
};

export default LoadingScreen;
